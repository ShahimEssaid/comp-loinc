# this turns on a fast run for dev mode
#--fast-run

-o
out-comploinc

builder
  set-module
    -n
    loinc-parts-list
  lp-inst-all
#    --active
  l-label
  l-annotate
  load-schema
      -f
      comp_loinc.yaml
  save-owl
