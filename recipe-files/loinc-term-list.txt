# this turns on a fast run for dev mode
#--fast-run

-o
out-comploinc

builder
  set-module
    -n
    loinc-terms-list
  lt-inst-all
    --active
  lt-parent
  l-label
  l-annotate
  load-schema
      -f
      comp_loinc.yaml
  save-owl
